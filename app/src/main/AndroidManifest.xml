<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quan_ly_thue_xe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/anh2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/anh2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quan_ly_thue_xe">
        <activity
            android:name=".Framentkhachhang.Menu"
            android:exported="false" />
        <activity
            android:name=".Account.RegisterAccountActivity"
            android:exported="false" />
        <activity
            android:name=".Account.RegisterInfoActivity"
            android:exported="false" />
        <activity
            android:name=".Account.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".InfomationActivity"
            android:exported="false" />
        <activity
            android:name=".Framentkhachhang.MainActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".StartScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>